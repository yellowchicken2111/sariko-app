# Claude Code Settings

## Permissions
- Read/search tools always allowed
- `npm run dev`, `npm run build`, `python main.py` auto-approved

## Personal Overrides
`settings.local.json` is gitignored — use it for machine-specific env or permission overrides.
